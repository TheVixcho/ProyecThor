#pragma once
#include <string>

namespace ProyecThor::UI {

    class SongView {
    public:
        SongView() = default;
        ~SongView() = default;

        void Render();
    };

} // namespace ProyecThor::UI